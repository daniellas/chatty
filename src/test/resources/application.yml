chatty:
  security:
    customerUsername: customer
    customerPassword: password
    customerRole: CUSTOMER
    employeeUsername: employee
    employeePassword: password
    employeeRole: EMPLOYEE
    adminUsername: admin
    adminPassword: admin
    adminRole: ACTUATOR
  repository:
    file:
      rootPath: target/reporoot

logging:
  level:
    root: INFO

endpoints:
  jmx:
    enabled: true

db:
  url: jdbc:h2:mem:testdb
  username: hrtools
  password: hrtools
  maxPoolSize: 20
