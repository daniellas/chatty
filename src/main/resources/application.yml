chatty:
  security:
    customerUsername: customer
    customerPassword: password
    employeeUsername: employee
    employeePassword: password
  repository:
    file:
      rootPath: /tmp/reporoot

logging:
  level:
    root: INFO

endpoints:
  jmx:
    enabled: true
